import express from "express";
// import isAuthenticated , {authorizeRole} from "../middleware/auth.js"

const router = express.Router()

router.route("/trainer/register").post()

export default router